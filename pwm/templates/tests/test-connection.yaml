apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pwm.fullname" . }}-test-connection"
  labels:
{{ include "pwm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pwm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
