apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "leo-chart.fullname" . }}-test-connection"
  labels:
{{ include "leo-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "leo-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
